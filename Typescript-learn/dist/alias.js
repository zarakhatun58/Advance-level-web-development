"use strict";
const crash1 = {
    name: 'Danish',
    age: 34,
    profession: 'service man',
    address: 'kolkata'
};
const crash2 = {
    name: 'Ansar',
    age: 36,
    profession: 'developer',
    address: 'kolkata'
};
const isCrushMarried = false;
const couseName = 'Next level Web development';
const calculate = (number1, number2, operation //(x,y)=> x-y) (10,20)=>10-20
) => {
    // return number1+number2
    return operation(number1, number2);
};
calculate(5, 7, (x, y) => x + y);
calculate(20, 4, (x, y) => x - y);
calculate(20, 5, (x, y) => x * y);
calculate(100, 4, (x, y) => x / y);
console.log(calculate);
